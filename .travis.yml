language:
    - python
python:
    - "3.5"

install:
    - git clone --depth=50 --branch=master https://github.com/swxEmily/repo.git ../../swxEmily/repo
    - cd ../../swxEmily/repo && git checkout && ls -alF
    - cd ../../ && ls -alF && cd swxEmily && ls -alF
    - cd proxy && ls -alF
    - python2 --version
    - python3 --version

script: 
    - g++ main.cc -o main && ./main
    - python main.py
